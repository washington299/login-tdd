import { SignUp } from '../../components/SignUp';

import * as S from './styles';

export const SignUpPage = () => {
    return (
        <S.Wrapper>
            <SignUp onSubmit={() => {}} />
        </S.Wrapper>
    )
}